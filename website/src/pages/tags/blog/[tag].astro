---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import BlogPost from '../../../components/BlogPost.astro';
import {generateTagRoute} from '../../../scripts/dynamicRoutes.ts'

export async function getStaticPaths() {
  return generateTagRoute("blog");
}

const { tag } = Astro.params;
const { items } = Astro.props;
---
<BaseLayout pageTitle={tag}>
  <p>Posts tagged with {tag}</p>
  <ul>
    <!-- {posts.map((post: any) =>  <BlogPost url={`/posts/${post.id}/`}  title={post.data.title}/>)} -->
    {items.map((item: any) =>  <BlogPost url={`/${item.id.replace("/","/posts/")}/`}  title={"ff" + item.data.title}/>)}
  </ul>
</BaseLayout>